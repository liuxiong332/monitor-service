
#spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:6446/schoolo2o?serverTimezone=UTC&useLegacyDatetimeCode=false
#spring.datasource.username=root
#spring.datasource.password=123456

spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/monitor_service?serverTimezone=UTC&useLegacyDatetimeCode=false
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:12345678}
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
  mail:
    host: smtp.163.com
    port: 25
    username: username
    password: password

    properties:
      mail:
        smtp:
          auth: true


mybatis:
  configuration:
    map-underscore-to-camel-case: true #开启驼峰映射


#mybatis-plus配置控制台打印完整带参数SQL语句
#mybatis-plus:
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl